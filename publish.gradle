subprojects {
    apply plugin: 'com.jfrog.bintray'

    bintray {
        user = project.hasProperty('bintrayUser') ? project.property('bintrayUser') : System.getenv('BINTRAY_USER')
        key = project.hasProperty('bintrayApiKey') ? project.property('bintrayApiKey') : System.getenv('BINTRAY_API_KEY')

        configurations = ['archives']

        publish = true

        dryRun = project.hasProperty('dryRun') ? project.property('dryRun') : false

        // override existing artifacts?
        override = false

        // metadata
        pkg {
            repo = 'cuba-components'
            name = 'translation-it'

            websiteUrl = 'https://github.com/pfurini/cuba-translation-it'
            issueTrackerUrl = 'https://github.com/pfurini/cuba-translation-it/issues'
            vcsUrl = 'https://github.com/pfurini/cuba-translation-it.git'
            githubRepo = 'pfurini/cuba-translation-it'
            githubReleaseNotesFile = 'CHANGELOG.md'
            publicDownloadNumbers = true
            licenses = ["Apache-2.0"]
            labels = ['cuba', 'cuba-platform', 'cuba-component', 'translation', 'localization', 'italian']
        }
    }
}